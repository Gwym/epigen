


export enum CollectionId { User, Session } 

// FIXME (1) :  server/persistor only information ? or needed by client for Indirection (yenah) 
// => shared/messaging?
// => declare only ?
// => enum extends

// export enum CollectionId { Indirection, User, Furniture, Agent, Cell, Session } 
